# AGENTS.md

## Lint / Type Check / Validation

- **Syntax check**: `node -c <file.js>` (no global linter configured)
- **Scenario validation**: `npm run validate:scenario`
- **Permutation generation**: `npm run permute`

## Commands

- `npm start` / `npm run dev` — start the dev server (`node server.js`)
